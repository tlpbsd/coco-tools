version: '3'

services:
  coco-dev:
    image: jamieleecho/coco-tools:0.0
    environment:
      - HOME=/home/$USER
    volumes:
      - $HOME/..:/home

  run-tests:
    image: jamieleecho/coco-tools:0.0
    volumes:
      - ./:/coco
    working_dir: /coco
