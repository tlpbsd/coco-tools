version: '3'

services:
  coco-dev:
    image: tlpbsd-coco-tools:latest
    environment:
      - HOME=/home/$USER
    volumes:
      - $HOME/..:/home

  run-tests:
    image: tlpbsd-coco-tools:latest
    volumes:
      - ./:/coco
    working_dir: /coco
